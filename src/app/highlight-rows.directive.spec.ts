import { HighlightRowsDirective } from './highlight-rows.directive';

describe('HighlightRowsDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightRowsDirective();
    expect(directive).toBeTruthy();
  });
});
